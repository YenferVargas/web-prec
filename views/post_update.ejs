<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Editar Artículo</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        .container {
            margin-top: 50px;
            box-shadow: 0px 0px 20px 0px rgba(0, 0, 0, 0.1);
            padding: 40px;
            border-radius: 10px;
            width: 70%;
        }
        form {
            padding: 40px;
            background-color: rgb(227, 224, 241);
        }

        .form-group {
            margin-bottom: 25px;
        }

        .form-label {
            font-weight: bold;
        }

        .form-control {
            border-radius: 5px;
        }

        .form-control:focus {
            border-color: #007bff;
            box-shadow: 0 0 0 0.2rem rgba(0, 123, 255, 0.25);
        }

        textarea.form-control {
            resize: none;
        }

        .center-text {
            text-align: center;
            margin-top: 2rem;
        }
    </style>
</head>

<body>
    <div class="container mt-5">
        <h1 class="center-text">Editar artículo</h1>
        <form method="POST" action="/posts_update/<%= articulo._id %>" class="mt-4">
            <div class="form-group mb-3">
                <label for="titulo" class="form-label">Título:</label>
                <input type="text" class="form-control" name="titulo" value="<%= articulo.titulo %>" required>
            </div>

            <div class="form-group mb-3">
                <label for="descripcion" class="form-label">Descripción:</label>
                <textarea class="form-control" name="descripcion" required><%= articulo.descripcion %></textarea>
            </div>

            <div class="form-group mb-3">
                <label for="categoria" class="form-label">Categoría:</label>
                <input type="text" class="form-control" name="categoria" value="<%= articulo.categoria %>" required>
            </div>

            <div class="form-group mb-3">
                <label for="fecha" class="form-label">Fecha:</label>
                <input type="date" class="form-control" name="fecha" value="<%= articulo.fecha %>" required>
            </div>

          

            <button type="submit" class="btn btn-primary text-start mt-auto">Guardar Cambios</button>
        </form>
        <a href="/posts_listar" class="mt-3 d-block">Volver a la lista de artículos</a>
    </div>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/js/bootstrap.min.js"></script>
</body>

</html>
